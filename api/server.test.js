/*

  note: integration tests would go here;

  mock require could allow a mock db to be used and then the api
  could be exercised with calls and known responses.

  any other external dependencies should be mocked in a similar way
  to allow testing.

  end of note

*/

test("Dummy Integration Test", () => {
  // dummy test to place hold for integration tests
  expect(1).not.toBe(0);
});
